local ui = require("data.scripts.prototypes.ui")
local terrain = require("data.scripts.prototypes.terrain")

prototypes = {}
prototypes.ui = ui;
prototypes.terrain = terrain;

print("Loaded prototypes.")