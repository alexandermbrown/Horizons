# DO NOT EDIT THIS FILE
#
# These are the default settings for the game and may break something if changed.
# Actual config is located at: (edit at your own risk)
# [Windows] %APPDATA%/Horizons/config/config.yaml
# [Mac]     ?
# [Linux]   ?

# Graphics
window_width: { type: int, default: 1280 }
window_height: { type: int, default: 720 }
use_vsync: { type: bool, default: false }

# Key binds (https://wiki.libsdl.org/SDLScancodeLookup)
keybind_move_left: { type: int, default: 4 }  # A
keybind_move_right: { type: int, default: 7 } # D
keybind_move_up: { type: int, default: 26 }   # W
keybind_move_down: { type: int, default: 22 } # S

locale: { type: int, default: 1 }
